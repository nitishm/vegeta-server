notifications:
  email: false

language: go

go:
  - 1.x

env:
  - GO111MODULE=on

install: true

before_script:
  - make install

script:
  - make lint
  - make validate
  - make ineffassign
  - make update-deps
  - GO111MODULE=on go test -covermode=count -coverprofile=profile.cov .

after_script:
  - goveralls -coverprofile=profile.cov -service=travis-ci
